import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryDark = Color(0xff191443);
  static const Color primaryLight = Color(0xffF2F8FF);

  // Text colors
  static const Color textDark = Color(0xff010203);
  static const Color textMedium = Color(0xff242424);
  static const Color textLight = Color(0xff919195);
  static const Color textMuted = Color(0xff54534A);
  static const Color textHint = Color(0xff828282);

  // Other colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;
}
